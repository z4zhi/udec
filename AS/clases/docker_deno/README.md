Crear carpeta app
code .
docker compose up --build

deno 
ctrl shift p initi

docker ps /a

localhost:8081

ir a postman

post: localhost:8080/api/quote
body
raw JSON
{
"quote": "wassup batman", "quoteID": "2", "author": "batwoman"
}
send
